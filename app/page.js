import React, { Fragment } from "react";

import Hero from "@/views/Hero";

const Home = () => {
  return (
    <Fragment>
      <section className="root-section">
        <Hero />
      </section>
    </Fragment>
  );
};

export default Home;
